services:
  - type: web
    name: game-api-ai
    runtime: python
    plan: starter
    region: frankfurt
    branch: main
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: sh scripts/download_model.sh && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    disk:
      name: ai-model-data
      mountPath: /var/data
      sizeGB: 5
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
      - key: MODEL_PATH
        value: /var/data/model.gguf
      - key: MODEL_URL
        value: https://huggingface.co/Qwen/Qwen3-4B-GGUF/resolve/main/Qwen3-4B-Q4_K_M.gguf?download=true
      - key: MODEL_N_CTX
        value: 4096
      - key: MODEL_MAX_TOKENS
        value: 512
